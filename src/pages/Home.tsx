import React from "react"
import {
  BlueButton,
  CyanButton,
  GreenButton,
  GreyButton,
  PinkButton,
  PurpleButton,
  RedButton,
  YellowButton,
} from "../utils/design/buttons"
import Flex from "../utils/design/flex"

const Home = ({}) => {
  return <h1>home</h1>
}

export default Home
